# Advancer — environment variables
# Copy to .env.local for local dev; set the same keys in Vercel → Project → Settings → Environment Variables.

# Supabase project "Advancer" (lzjxkfonvdsxkfzfmvph)
NEXT_PUBLIC_SUPABASE_URL=https://lzjxkfonvdsxkfzfmvph.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx

# Optional — only if you later add admin/server tasks that bypass RLS.
# SUPABASE_SERVICE_ROLE_KEY=
# Optional — only if you run Drizzle directly against Postgres (we ship via the Supabase MCP).
# DATABASE_URL=
